---
phase: 01-app-shell-content-deployment
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/content/about.ts
  - src/content/skills.ts
  - src/content/experience.ts
  - src/content/certifications.ts
  - src/content/education.ts
  - src/sections/About.tsx
  - src/sections/Skills.tsx
  - src/sections/Experience.tsx
  - src/sections/Certifications.tsx
  - src/sections/Education.tsx
  - src/sections/About.test.tsx
  - src/sections/Skills.test.tsx
  - src/sections/Experience.test.tsx
  - src/sections/Certifications.test.tsx
  - src/sections/Education.test.tsx
  - src/App.tsx
  - src/nav/TopNav.tsx
autonomous: true
requirements: [ABOUT-01, EXP-01, EXP-02, SKILL-01, SKILL-02, CERT-01, EDU-01]
must_haves:
  truths:
    - "Visitor can read a synthesized About summary sourced from the resume's Professional Summary (ABOUT-01)."
    - "About section renders a single static professional-summary paragraph, with no dynamic/loading/error states (ABOUT-01, resolved unclassified edge)."
    - "Visitor can read a chronological Experience timeline covering all 7 resume roles, most-recent-first, with quantified achievement bullets per role (EXP-01, EXP-02)."
    - "Each Experience role renders as a distinct timeline entry regardless of date adjacency to neighboring roles — no merge/collide logic exists (EXP-02 adjacency edge, resolved)."
    - "Achievement bullet lists render whatever number of bullets exist per role (1 to 6), with no minimum enforced (EXP-02 empty edge, resolved)."
    - "Bullets within a role render in fixed, stable source order matching the resume (EXP-02 ordering edge, resolved)."
    - "Skills are grouped by the resume's 10 categories as tag/chip groupings — never percentage bars or numeric proficiency scores (SKILL-01, SKILL-02)."
    - "Certifications section lists all 6 resume certifications in fixed resume order (CERT-01)."
    - "Education section lists both resume degrees in fixed order, most-recent-first (Master's before Bachelor's, matching the resume's own listed order) (EDU-01)."
  artifacts:
    - src/content/about.ts
    - src/content/skills.ts
    - src/content/experience.ts
    - src/content/certifications.ts
    - src/content/education.ts
    - src/sections/About.tsx
    - src/sections/Skills.tsx
    - src/sections/Experience.tsx
    - src/sections/Certifications.tsx
    - src/sections/Education.tsx
    - src/sections/About.test.tsx
    - src/sections/Skills.test.tsx
    - src/sections/Experience.test.tsx
    - src/sections/Certifications.test.tsx
    - src/sections/Education.test.tsx
  key_links:
    - "App.tsx renders About, Experience, Skills, Certifications, Education (in that order) between Hero and the Plan 03 sections"
    - "TopNav's links array gains 5 new anchor entries matching the 5 new section ids"
  prohibitions:
    - statement: "Skills section must never render percentage bars, progress meters, or numeric proficiency scores (SKILL-02)."
      status: "descriptor-less"
      verification: "grep -rin 'progress\\|proficiency\\|%[0-9]' src/sections/Skills.tsx src/content/skills.ts returns no matches (excluding this comment string itself)"
  backstop:
    - statement: "Experience and Skills entry counts (7 roles, 10 categories) are resume-derived, not locked counts the way Testimonials/Projects/Certifications/Education are — the same auto-fit/wrap layout must hold for any count without a design pass dedicated to this exact number."
      verification: "backstop"
    - statement: "01-UI-SPEC.md's populated-state note says 'Experience (8 roles, vertical timeline)' — the actual resume has exactly 7 distinct roles (verified directly against ~/Downloads/Sandeep_Gupta_Platform_Engineering.pdf on 2026-07-19). Treat the resume as ground truth per D-01; the layout must not assume a specific count."
      verification: "backstop"
---

<objective>
Add 5 static, resume-derived content sections — About, Experience, Skills, Certifications, Education — as three vertical slices, each wiring a typed content module through a presentational component into the app shell built in Plan 01.

Purpose: Delivers ABOUT-01, EXP-01, EXP-02, SKILL-01, SKILL-02, CERT-01, EDU-01 — the "resume-as-source-of-truth" half of the phase's content (D-01: no additional synthesis beyond what's in the resume).

Output: `src/content/{about,skills,experience,certifications,education}.ts` + matching `src/sections/*.tsx` + tests, wired into `App.tsx` and `TopNav.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-app-shell-content-deployment/01-CONTEXT.md
@.planning/phases/01-app-shell-content-deployment/01-UI-SPEC.md
@.planning/phases/01-app-shell-content-deployment/01-PATTERNS.md
@.planning/phases/01-app-shell-content-deployment/01-01-SUMMARY.md
</context>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Notes |
|---|---|---|
| `src/content/about.ts` | content | `AboutContent` interface + `about` const (single `summary: string` field) |
| `src/content/skills.ts` | content | `SkillCategory` interface (`category: string; items: string[]`) + `skills: SkillCategory[]` (10 entries) |
| `src/content/experience.ts` | content | `ExperienceRole` interface (`role, company, location, start, end, achievements: string[]`) + `experience: ExperienceRole[]` (7 entries, most-recent-first) |
| `src/content/certifications.ts` | content | `certifications: string[]` (6 entries, fixed resume order) |
| `src/content/education.ts` | content | `EducationEntry` interface (`institution, degree, date`) + `education: EducationEntry[]` (2 entries, most-recent-first) |
| `src/sections/About.tsx` | component | renders `about.summary` in `<section id="about">` |
| `src/sections/Skills.tsx` | component | renders `skills` as category groups of tag chips in `<section id="skills">` |
| `src/sections/Experience.tsx` | component | renders `experience` as a vertical timeline in `<section id="experience">` |
| `src/sections/Certifications.tsx` | component | renders `certifications` as a list in `<section id="certifications">` |
| `src/sections/Education.tsx` | component | renders `education` as a list in `<section id="education">` |
| `src/sections/{About,Skills,Experience,Certifications,Education}.test.tsx` | tests | render + content assertions per section |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: About + Skills</name>
  <files>src/content/about.ts, src/content/skills.ts, src/sections/About.tsx, src/sections/Skills.tsx, src/sections/About.test.tsx, src/sections/Skills.test.tsx, src/App.tsx, src/nav/TopNav.tsx</files>
  <read_first>
    - `.planning/phases/01-app-shell-content-deployment/01-PATTERNS.md` — content-module + presentational-component file pattern.
    - `.planning/phases/01-app-shell-content-deployment/01-UI-SPEC.md` — card/tag-chip styling tokens for the Skills grid; confirms SKILL-02's "no percentage bars" constraint visually.
    - `src/content/hero.ts`, `src/sections/Hero.tsx` (Plan 01) — the exact interface+const module pattern and section-component pattern to replicate.
    - `src/App.tsx`, `src/nav/TopNav.tsx` (Plan 01) — current composition and links array to extend.
  </read_first>
  <behavior>
    - `About.test.tsx`: renders `<About />`, asserts `screen.getByText(about.summary)` (or a substring match if styled across multiple nodes) is truthy.
    - `Skills.test.tsx`: renders `<Skills />`, asserts all 10 category labels from `skills` are present in the rendered output, and asserts no element's text content matches a `%` sign or the word "proficiency".
  </behavior>
  <action>
    Create `src/content/about.ts`: `export interface AboutContent { summary: string }` and `export const about: AboutContent = { summary: '...' }` where the summary is: "Sandeep Gupta is a Senior Site Reliability Engineer and platform tool builder with expertise in cloud infrastructure, automation, observability, and developer experience. He currently focuses on building systems and tools that use AI to drive cost optimization, CI/CD standardization, and automated microservice deployments, and is adept at guiding teams in SRE best practices to improve reliability, scalability, and developer productivity. Across roles at Adobe, Carbonite, and Workfront, he has consistently focused on making large-scale systems more reliable, observable, and easier for other engineers to build on. He holds a Master's in Information Systems from the University of Utah and a Bachelor's in Computer Science & Engineering, and is certified in Python programming, Jenkins engineering, AWS Solutions Architecture, Azure fundamentals, ITIL V3, and Datadog." (Directly synthesized from the resume's Professional Summary + Education + Certifications sections per D-01 — no facts added beyond what the resume states.)

    Create `src/content/skills.ts`: `export interface SkillCategory { category: string; items: string[] }` and `export const skills: SkillCategory[] = [...]` with exactly these 10 entries, in this order, items verbatim from the resume: `{ category: 'Operating Systems', items: ['Unix', 'Linux (Ubuntu)', 'Linux (RHEL)', 'Linux (CentOS)', 'Windows'] }`, `{ category: 'Programming & Scripting', items: ['Python', 'Bash', 'JavaScript', 'React', 'TypeScript'] }`, `{ category: 'Web Services', items: ['Nginx', 'Kong', 'Traefik', 'Apache', 'IIS'] }`, `{ category: 'Database', items: ['MySQL', 'Postgres', 'MongoDB'] }`, `{ category: 'IaC', items: ['Terraform', 'Ansible', 'Chef', 'Vagrant'] }`, `{ category: 'GitOps', items: ['ArgoCD', 'Argo Workflows', 'Argo Rollouts'] }`, `{ category: 'DevOps Tools', items: ['Docker', 'Kubernetes', 'Helm', 'Artifactory', 'GitLab', 'Git', 'Jenkins'] }`, `{ category: 'Observability', items: ['Datadog', 'OpenTelemetry', 'OTEL Collector', 'Vector', 'Splunk', 'Graylog'] }`, `{ category: 'Cloud Technologies', items: ['AWS', 'Azure', 'GCP'] }`, `{ category: 'AI Tools and Frameworks', items: ['Cursor', 'Claude', 'Ollama', 'Exo', 'RAG', 'MCP'] }`.

    Create `src/sections/About.tsx`: `<section id="about" aria-labelledby="about-heading">` with an `<h2 id="about-heading">About</h2>` and a `<p>{about.summary}</p>`.

    Create `src/sections/Skills.tsx`: `<section id="skills" aria-labelledby="skills-heading">` mapping `skills` to a grid of category blocks, each rendering the category name as a sub-heading and `items` as a wrapped row of tag-chip `<span>` elements (per `01-UI-SPEC.md`'s tag-chip token) — no bar, meter, or percentage element anywhere in this component (SKILL-02).

    Update `src/App.tsx`: insert `<About />` immediately after `<Hero />` and before `<Contact />`; insert `<Skills />` after `<About />` (Experience/Certifications/Education land between them in Task 2/3 of this same plan — order the JSX so the final section order across all of Plan 02's tasks reads Hero → About → Experience → Skills → Certifications → Education, matching `01-RESEARCH.md`'s recommended section order).

    Update `src/nav/TopNav.tsx`'s `links` array: insert `{ href: '#about', label: 'About' }` and `{ href: '#skills', label: 'Skills' }` at the correct positions (About right after Home, Skills after Experience/before Certifications — Experience's nav entry is added in Task 2).
  </action>
  <verify>
    <automated>npm test -- --run src/sections/About.test.tsx src/sections/Skills.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "category:" src/content/skills.ts` returns `10`.
    - `grep -in 'progress\|proficiency' src/sections/Skills.tsx | grep -v '^\s*//' | wc -l` returns `0`.
    - `npm test -- --run src/sections/About.test.tsx src/sections/Skills.test.tsx` exits 0.
  </acceptance_criteria>
  <done>About and Skills sections render real resume-derived content, are wired into App.tsx/TopNav.tsx, and both test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Experience</name>
  <files>src/content/experience.ts, src/sections/Experience.tsx, src/sections/Experience.test.tsx, src/App.tsx, src/nav/TopNav.tsx</files>
  <read_first>
    - `.planning/phases/01-app-shell-content-deployment/01-UI-SPEC.md` — vertical-timeline styling tokens and the "partial" state note (a role with only 1 bullet must render correctly).
    - `src/content/skills.ts`, `src/sections/Skills.tsx` (Task 1 of this plan) — pattern to replicate for a second content module in the same plan.
  </read_first>
  <behavior>
    - `Experience.test.tsx`: renders `<Experience />`, asserts all 7 role/company pairs from `experience` appear in the rendered output, asserts the roles appear in the array's authored order (most-recent-first), and asserts a role with a single achievement (e.g. the Intermountain entry) still renders its one bullet without error.
  </behavior>
  <action>
    Create `src/content/experience.ts`: `export interface ExperienceRole { role: string; company: string; location: string; start: string; end: string; achievements: string[] }` and `export const experience: ExperienceRole[] = [...]` with exactly these 7 entries, most-recent-first (matching the resume's own listed order):

    1. `{ role: 'Senior Site Reliability Engineer', company: 'Adobe', location: 'Lehi, Utah', start: 'May 2021', end: 'Present', achievements: [ "Built and scaled Adobe Workfront's Backstage-based Internal Developer Portal for 250+ engineers — a 30-plugin React 18/Node.js platform unifying ArgoCD deployments, Kubernetes rightsizing, Split.io feature flags, SLOs, service maturity, Datadog CCM cost/reliability insights and error-budget health, Vault, Okta SSO, and GitLab CI/CD into a single service-owner view.", "Contributed to Cadence, Adobe Workfront's Kubernetes/Temporal-based AI agent orchestration platform, adding dependency graph views, automated MR reviews, and cross-provider sub-agent dispatch with isolated pod execution and durable streamed results.", "Led zero-downtime ArgoCD sharding for 568 ApplicationSets, 4,200+ Applications, and 252 services, splitting one control plane into 4 shards across 8+ repos and delivering a 15x UI latency improvement (10-30s to p95 under 2s) while maintaining 99%+ sync success and ~3x capacity headroom.", "Built Argo Workflows and ArgoCD deployment automation for 1,300+ microservices and migrated 90%+ of services from GitLab CD to ArgoCD, achieving 98.4% deployment success, cutting deployment time by 50-67% through batch orchestration and smart sync optimization, and standardizing GitLab CI/CD templates to 80%+ adoption.", "Architected Datadog and Vector log-governance pipelines across multi-region Kubernetes and owned enterprise observability standards for logs, metrics, traces, and RUM, reducing observability spend by 15-20%, raising tagging compliance from 40% to 100%, and driving 80%+ telemetry adoption through reusable Datadog and OpenTelemetry onboarding modules.", "Introduced reusable Helm chart standards adopted by 20%+ of services, reducing Kubernetes deployment variance and improving service onboarding consistency." ] }`
    2. `{ role: 'Site Reliability Engineer', company: 'Carbonite Inc. (An Opentext Company)', location: 'Salt Lake City, Utah', start: 'Sep 2019', end: 'Mar 2021', achievements: [ "Automated infrastructure configuration with Chef, Jenkins, and Git-based CI/CD, improving repeatability across system and application deployments.", "Built centralized logging and monitoring platforms using Kubernetes, Kubeadm, Helm, Graylog, Zabbix, SolarWinds, Icinga2, and Dell OpenManage for engineering and support teams." ] }`
    3. `{ role: 'System Engineer', company: 'Workfront', location: 'Lehi, Utah', start: 'Nov 2018', end: 'Jun 2019', achievements: [ "Built AWS-based CI/CD and platform infrastructure using GitLab, Jenkins, TravisCI, Terraform, Kubernetes, Artifactory, Nginx Ingress, Cert Manager, Consul, and Vault.", "Automated security and infrastructure workflows using Python, AWS Lambda, Splunk, GuardDuty, CloudWatch, Slack, Samanage, and Workfront APIs, reducing manual effort for security and infrastructure teams." ] }`
    4. `{ role: 'DevOps Intern', company: 'Workfront', location: 'Lehi, Utah', start: 'Jul 2018', end: 'Nov 2018', achievements: [ "Developed and managed Python-based automations using AWS Lambda to streamline multiple workflows based on SecurityCenter, Splunk, AWS GuardDuty, AWS CloudWatch, Slack, and Samanage." ] }`
    5. `{ role: 'DevOps Intern', company: 'Intermountain', location: 'West Valley, Utah', start: 'Jan 2018', end: 'Jul 2018', achievements: [ "Developed patch automation for Windows servers and reusable Ansible configuration to retrieve and renew Let's Encrypt TLS certificates using AWS Route53 for domain validation, and developed and maintained an internal web portal using HTML, CSS, PHP, Laravel, and Azure MS SQL." ] }`
    6. `{ role: 'Infrastructure Engineer', company: 'BOLD Technology Solutions Pvt. Ltd.', location: 'Noida, India', start: 'Feb 2016', end: 'Jul 2017', achievements: [ "Managed AWS production infrastructure across EC2, Auto Scaling, EBS, S3, Route53, IAM, CloudWatch, and New Relic, and automated infrastructure inventory, health checks, dashboards, and operational reporting with Python, CloudWatch, and New Relic." ] }`
    7. `{ role: 'Senior Analyst, Infrastructure', company: 'HCL Technologies', location: 'Noida, India', start: 'Jun 2013', end: 'Jan 2016', achievements: [ "Supported Linux and Windows HA infrastructure, monitoring CPU, memory, disk, and network utilization across virtualized enterprise systems, and managed service requests, health checks, incident workflows, and operational reporting using ServiceNow, HPSM/HPSC, HP OVO, and Informatica." ] }`

    Note: this is 7 roles, not the 8 estimated in `01-UI-SPEC.md`'s populated-state note — the resume (ground truth per D-01) has exactly 7. Do not pad to 8.

    Create `src/sections/Experience.tsx`: `<section id="experience" aria-labelledby="experience-heading">` mapping `experience` to a vertical timeline: each entry renders `role`, `company`, `location`, a `${start} – ${end}` date range, and an unordered list of `achievements` (renders correctly whether the list has 1 or 6 items — no minimum-count assumption in the markup).

    Update `src/App.tsx`: insert `<Experience />` between `<About />` and `<Skills />` (final Plan-02 order: Hero → About → Experience → Skills → Certifications → Education).

    Update `src/nav/TopNav.tsx`'s `links` array: insert `{ href: '#experience', label: 'Experience' }` between the About and Skills entries.
  </action>
  <verify>
    <automated>npm test -- --run src/sections/Experience.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "role:" src/content/experience.ts` returns `7`.
    - `grep -c "'Present'" src/content/experience.ts` returns `1` (only the current Adobe role is open-ended).
    - `npm test -- --run src/sections/Experience.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Experience renders all 7 roles most-recent-first with correct achievement bullets (1 to 6 per role), wired into App.tsx/TopNav.tsx, test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Certifications + Education</name>
  <files>src/content/certifications.ts, src/content/education.ts, src/sections/Certifications.tsx, src/sections/Education.tsx, src/sections/Certifications.test.tsx, src/sections/Education.test.tsx, src/App.tsx, src/nav/TopNav.tsx</files>
  <read_first>
    - `src/content/experience.ts`, `src/sections/Experience.tsx` (Task 2 of this plan) — list-rendering pattern to replicate for two smaller, simpler content modules.
  </read_first>
  <behavior>
    - `Certifications.test.tsx`: renders `<Certifications />`, asserts all 6 certification strings from `certifications` appear in the rendered output.
    - `Education.test.tsx`: renders `<Education />`, asserts both `education` entries appear, and asserts the Master's entry (University of Utah) renders before the Bachelor's entry (Uttar Pradesh Technical University) in DOM order.
  </behavior>
  <action>
    Create `src/content/certifications.ts`: `export const certifications: string[] = [ 'Certified Associate in Python Programming (PCAP-31-03)', 'Certified Jenkins Engineer', 'AWS Certified Solutions Architect – Associate (CSAA)', 'Microsoft Azure Information Systems (MS Azure-IS)', 'ITIL V3 Foundation', 'Datadog' ]` — exactly 6 entries, verbatim from the resume's Certifications line (fixed resume order, no reordering).

    Create `src/content/education.ts`: `export interface EducationEntry { institution: string; degree: string; date: string }` and `export const education: EducationEntry[] = [ { institution: 'University of Utah', degree: 'Master of Science in Information Systems', date: 'Aug 2018' }, { institution: 'Uttar Pradesh Technical University', degree: 'Bachelor of Technology in Computer Science & Engineering', date: 'Jun 2013' } ]` — most-recent-first, matching the resume's own listed order (no discretion conflict).

    Create `src/sections/Certifications.tsx`: `<section id="certifications" aria-labelledby="certifications-heading">` mapping `certifications` to a simple tag/card list (no percentage/score element, consistent with the site's no-quantified-proficiency-graphics stance).

    Create `src/sections/Education.tsx`: `<section id="education" aria-labelledby="education-heading">` mapping `education` to a list of `institution` / `degree` / `date` entries in array order.

    Update `src/App.tsx`: insert `<Certifications />` and `<Education />` after `<Skills />`, completing Plan 02's final order: Hero → About → Experience → Skills → Certifications → Education (Plan 03 appends Projects, Testimonials, and Contact stays last per Plan 01).

    Update `src/nav/TopNav.tsx`'s `links` array: append `{ href: '#certifications', label: 'Certifications' }` and `{ href: '#education', label: 'Education' }` after the Skills entry and before the Contact entry.
  </action>
  <verify>
    <automated>npm test -- --run src/sections/Certifications.test.tsx src/sections/Education.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'" src/content/certifications.ts` shows exactly 6 top-level string entries (count array items, not quote chars — verify by `node -e "console.log(require('./src/content/certifications.ts'))"` is not applicable for `.ts`; instead assert via the test file's `expect(certifications.length).toBe(6)`).
    - `npm test -- --run src/sections/Certifications.test.tsx src/sections/Education.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Certifications (6) and Education (2, most-recent-first) render, are wired into App.tsx/TopNav.tsx, both tests pass, and Plan 02's full section order (Hero→About→Experience→Skills→Certifications→Education) is correct in App.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new in this plan | About/Experience/Skills/Certifications/Education are static, build-time-authored, read-only content with no external links, no user input, and no runtime data fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Resume-derived content (About/Experience/Skills/Certifications/Education) is publicly visible on the deployed site | low | accept | Intentional and required — this is a public professional portfolio built specifically to be read by recruiters/hiring managers (PROJECT.md Core Value). All content here is already public via the user's LinkedIn/resume for job-seeking purposes; no phone number or other non-locked PII is included (see Plan 01's `contact.ts`, which deliberately omits the resume's phone number). |
</threat_model>

<verification>
1. `npm test -- --run` (full suite) passes, including all 5 new test files from this plan.
2. `npm run build` still exits 0 after these additions.
3. Manual read of `src/App.tsx` confirms section order: Hero → About → Experience → Skills → Certifications → Education (Contact remains last, per Plan 01).
4. `src/nav/TopNav.tsx`'s `links` array has 7 entries after this plan (Home, About, Experience, Skills, Certifications, Education, Contact).
</verification>

<success_criteria>
- A visitor can read the About summary, browse all 7 Experience roles with achievement bullets, see Skills grouped by 10 categories (no percentage graphics), and see all 6 Certifications and both Education entries.
- TopNav navigates to all 5 new sections via matching anchor links.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-content-deployment/01-02-SUMMARY.md` when done
</output>
