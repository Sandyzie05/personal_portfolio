---
phase: 01-app-shell-content-deployment
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - vite.config.ts
  - README.md
autonomous: false
requirements: [DEPLOY-01]
user_setup:
  - service: github
    why: "New repository to host this project's source (separate from jagdamba_automobiles, per D-06)."
    dashboard_config:
      - task: "Confirm repository visibility (public, matching jagdamba_automobiles's convention) and the exact repo name (resolved in Task 1's decision checkpoint)."
        location: "github.com/Sandyzie05 (new repo)"
  - service: milesweb-cpanel
    why: "New Node.js application root for this project (D-06) — no CLI/API access exists for cPanel Node.js app setup; this step is manual per jagdamba_automobiles/README.md's proven process."
    dashboard_config:
      - task: "Setup Node.js App: set Application root to the new repo's clone path, startup file to app.js, Node.js version to 18 or 20, run NPM Install, run npm run build (or upload dist/), then restart the app."
        location: "cPanel -> Setup Node.js App"
must_haves:
  truths:
    - "The site is live at its MilesWeb production URL (DEPLOY-01)."
    - "Deployment mirrors the jagdamba_automobiles pattern exactly: dist/ built via Vite, served by the dependency-free app.js in a cPanel Node.js application root (D-05, D-06, D-07)."
    - "Post-deploy smoke test confirms the live root URL returns 200 and /resume.pdf returns 200 with Content-Type: application/pdf."
  artifacts:
    - vite.config.ts (base path finalized per D-08 resolution)
    - README.md (deployment notes for this project, adapted from jagdamba_automobiles's README.md)
  key_links:
    - "The new GitHub repo (name resolved in Task 1) is the source cPanel's 'Setup Node.js App' clones into its application root"
    - "app.js is the configured cPanel startup file; Node.js version selected in cPanel matches package.json's engines: ^18.0.0 || >=20.0.0"
    - "vite.config.ts's base value matches the exact URL path the site is served from (domain root '/' or a subpath, per D-08's resolution)"
  backstop:
    - statement: "Live production reachability is verified only by the manual scripts/deploy-smoke.sh curl check run post-deploy against the live URL, not by a compiled unit test."
      verification: "backstop"
---

<objective>
Resolve the deployment target (D-08: domain/subdomain + new GitHub repo name), then deploy the completed 9-section app shell to MilesWeb shared hosting via cPanel, mirroring the `jagdamba_automobiles` dependency-free `app.js` + Vite `dist/` pattern exactly (D-05/D-06/D-07), and prove it live with the same `scripts/deploy-smoke.sh` check used locally in Plan 01.

Purpose: Delivers DEPLOY-01 and closes ROADMAP.md Phase 1 Success Criterion 5 — the final remaining criterion after Plan 03 completed all content sections.

Output: A new GitHub repository, a live MilesWeb Node.js application serving this project's `dist/` via `app.js`, and a passing post-deploy smoke test against the live URL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-content-deployment/01-CONTEXT.md
@.planning/phases/01-app-shell-content-deployment/01-RESEARCH.md
@.planning/phases/01-app-shell-content-deployment/01-03-SUMMARY.md
@/Users/sandgupt/RandomIdeasWithAI/jagdamba_automobiles/README.md
</context>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Notes |
|---|---|---|
| New GitHub repository | infra | name resolved in Task 1; hosts this project's source, separate from `jagdamba_automobiles` (D-06) |
| `vite.config.ts` (updated) | config | `base` finalized to match the resolved deployment path (domain root `/` or a subpath) |
| `README.md` | docs | this project's deployment notes, adapted from `jagdamba_automobiles/README.md`'s "Git vs server" and "cPanel + GitHub" sections |
| Live MilesWeb cPanel Node.js application | infra | serves this project's `dist/` via `app.js`, matching D-05/D-06/D-07 |

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Resolve D-08 — deployment domain/subdomain placement and new GitHub repository name</name>
  <action>Ask the human to choose between domain-root and subpath hosting, and to supply the exact domain/subdomain/subpath string and new GitHub repository name — Claude cannot safely default this (a guessed domain the user does not own, or an unwanted repo name, would be actively wrong, not just conservative).</action>
  <decision>Resolve D-08: deployment domain/subdomain placement and new GitHub repository name</decision>
  <context>
    `01-CONTEXT.md`'s D-08 and `.planning/STATE.md`'s blockers both flag this as unresolved: the target domain/subdomain and the new GitHub repo name were deferred to "before DEPLOY-01 execution, not before" — that point is now. `vite.config.ts`'s `base` value (currently `'/'`, set in Plan 01 assuming domain-root hosting) must match whichever option is chosen before the production build is finalized.
  </context>
  <options>
    <option id="domain-root">
      <name>Domain root (e.g., a new domain, or a subdomain like portfolio.yourdomain.com pointed at its own cPanel Node.js app)</name>
      <pros>`vite.config.ts`'s `base: '/'` (already set in Plan 01) needs no change; simplest URL for a professional portfolio (no subpath to remember/share).</pros>
      <cons>Requires either registering/pointing a new domain or configuring a subdomain in cPanel before the Node.js app root can be created.</cons>
    </option>
    <option id="subpath">
      <name>Subpath under an existing domain (e.g., yourdomain.com/portfolio/), mirroring jagdamba_automobiles's own /jagdambaautomobiles/ pattern</name>
      <pros>No new domain/subdomain DNS configuration needed — reuses an existing, already-working domain.</pros>
      <cons>Requires changing `vite.config.ts`'s `base` from `'/'` to `'/<subpath>/'` and rebuilding (mirrors the `BASE` stripping logic Plan 01 deliberately removed from `app.js` — will need to be re-added if this option is chosen, since app.js currently assumes root-level serving).</cons>
    </option>
  </options>
  <resume-signal>Reply with: chosen option id ("domain-root" or "subpath"), the exact domain/subdomain/subpath string to use, and the new GitHub repository name (e.g., "domain-root, portfolio.sandeepgupta.dev, sandeep-portfolio").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: GitHub repo, base-path finalization, and production build</name>
  <files>vite.config.ts, README.md</files>
  <read_first>
    - `/Users/sandgupt/RandomIdeasWithAI/jagdamba_automobiles/README.md` — the exact "Git vs server", "Commands", and "cPanel + GitHub" sections to adapt for this project's own README.md.
    - `vite.config.ts` (created in Plan 01) — current `base: '/'` value to update only if Task 1's decision was "subpath".
    - `app.js` (patched in Plan 01) — if Task 1's decision was "subpath", this file needs the `BASE`-prefix-stripping block re-added (the exact inverse of the removal done in Plan 01 Task 2, but re-applied with the new resolved subpath value and keeping the path-containment/MIME fixes from Plan 01 intact).
  </read_first>
  <action>
    Create the new GitHub repository using the exact name resolved in Task 1 (`gh repo create Sandyzie05/<resolved-name> --public --source=. --remote=origin` or equivalent), matching `jagdamba_automobiles`'s convention of a public repo under the same `Sandyzie05` account.

    If Task 1 resolved to "domain-root": leave `vite.config.ts`'s `base: '/'` unchanged (already correct from Plan 01).

    If Task 1 resolved to "subpath": update `vite.config.ts`'s `base` to `'/<resolved-subpath>/'` (leading and trailing slash, matching `jagdamba_automobiles`'s exact convention), and re-add a `BASE`-prefix-stripping block to `app.js` (declare `const BASE = '/<resolved-subpath>'`, strip it from `urlPath` at the top of the request handler) — WITHOUT reintroducing the admin-routing block or the un-contained `path.join` that Plan 01 deliberately removed/fixed; the path-containment check and `.pdf` MIME entry from Plan 01 must remain intact.

    Push all committed source (excluding `node_modules/` and, per the reference project's convention, `dist/` unless the cPanel host cannot run a build server-side — default to NOT committing `dist/`, matching `jagdamba_automobiles`'s default `.gitignore`) to the new repo's `main` branch.

    Run `npm run build` locally one final time against the finalized `base` value, confirming `dist/index.html`'s asset paths reflect the resolved base path.

    Create this project's `README.md`, adapting `jagdamba_automobiles/README.md`'s structure: repository layout diagram, the "Git vs server" table (what gets committed vs. what's server-only), the `npm install` / `npm run build` / `npm start` command list, and a "cPanel + GitHub" numbered manual-steps section rewritten for this project's resolved repo name and application-root path.
  </action>
  <verify>
    <automated>npm run build && grep -c "base" vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 using the finalized `base` value.
    - `git remote -v` shows the new repository as `origin`.
    - `git log origin/main` (after push) contains this phase's commits.
    - If "subpath" was chosen: `grep -c "BASE" app.js` returns at least `1`, and `grep -c "path.resolve" app.js` still returns at least `1` (containment check preserved).
  </acceptance_criteria>
  <done>New GitHub repo exists and contains this project's source; vite.config.ts's base matches the resolved deployment path; production build succeeds; README.md documents the deployment process for this project.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Manual cPanel Node.js app setup and deployment</name>
  <action>In cPanel's "Setup Node.js App" UI (no CLI/API exists for this step), create and start the Node.js application that serves this project's build — Claude has already produced the production-ready `dist/` build, the dependency-free `app.js`, and pushed both to the new GitHub repository in Task 2; only the cPanel-side application setup itself requires a human at the keyboard.</action>
  <instructions>
    Already automated by Task 2: the finalized `dist/` build, the dependency-free `app.js` (matching the exact `jagdamba_automobiles` cPanel deployment shape), and a push of both to the new GitHub repository.

    The one manual step, following `jagdamba_automobiles/README.md`'s proven numbered process (adapted to this project's new repo/app root):
    1. In cPanel's Git Version Control (or SSH, if available), clone the new GitHub repo into `repositories/<resolved-repo-name>/`.
    2. In cPanel's "Setup Node.js App", create a new application: **Application root** = the relative path to that clone (e.g., `repositories/<resolved-repo-name>`, no leading `/home/.../`), **Application URL** = the domain/subdomain/subpath resolved in Task 1, **Application startup file** = `app.js`, **Node.js version** = 18 or 20 (matching `package.json`'s `engines`).
    3. Open the app's "Run NPM Install" button (full install, not production-only, since `tsc`/`vite` are devDependencies needed to build).
    4. Run `npm run build` via the cPanel terminal/SSH in that application root (or, if no build capability exists there, build locally and upload `dist/` via File Manager/FTP).
    5. Restart the Node.js app from cPanel's "Setup Node.js App" page so it picks up the new `dist/`.
    6. Confirm the app shows status "Running" in cPanel.
  </instructions>
  <verification>Once a live URL is shared, Task 4 runs `scripts/deploy-smoke.sh` against it and confirms the root URL returns HTTP 200 and `/resume.pdf` returns HTTP 200 with `Content-Type: application/pdf`.</verification>
  <resume-signal>Reply "deployed" once the cPanel Node.js app shows "Running" and you can share the live URL — or describe what step failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Post-deploy smoke verification</name>
  <files></files>
  <read_first>
    - `scripts/deploy-smoke.sh` (created in Plan 01) — reused as-is against the live URL, no changes needed.
  </read_first>
  <action>
    Run `bash scripts/deploy-smoke.sh &lt;live-URL-from-Task-3-resume-signal&gt;` against the now-live MilesWeb URL, confirming both checks pass: root URL returns HTTP 200, and `/resume.pdf` (or `/&lt;subpath&gt;/resume.pdf` if "subpath" was chosen) returns HTTP 200 with `Content-Type: application/pdf`.

    Spot-check (manually, via curl or a browser) that all 9 sections render, TopNav anchors navigate correctly, and at least one Contact action (mailto/LinkedIn/GitHub/resume download) works against the live URL — closing ROADMAP.md Phase 1's Success Criterion 5 and the phase's overall goal.
  </action>
  <verify>
    <automated>bash scripts/deploy-smoke.sh "$LIVE_URL"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/deploy-smoke.sh` exits 0 against the live URL.
    - The live root URL's response includes recognizable rendered content (e.g., `curl -s "$LIVE_URL" | grep -c "Sandeep Gupta"` returns at least `1`).
  </acceptance_criteria>
  <done>The live MilesWeb site passes both smoke checks; ROADMAP.md Phase 1's 5th and final Success Criterion is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub repo (public) → cPanel clone | Anything committed becomes publicly readable; must not include secrets or non-locked PII |
| cPanel Node.js application configuration | Manual, human-driven setup step with no audit trail beyond cPanel's own UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|-----------------|
| T-01-07 | Information Disclosure | Public GitHub repo could expose resume PII (phone number) or deployment specifics if committed carelessly | medium | mitigate | The resume's phone number was never added to any `content/*.ts` module (Plan 01's `contact.ts` deliberately omits it); no `.env`/secrets exist in this phase to leak; repo scope stays limited to professional-portfolio source only. |
| T-01-08 | Tampering | cPanel Node app configuration drift / stale deploy (app not restarted after a new build) | medium | mitigate | Explicit "restart the Node.js app" step in Task 2's manual checklist, followed by Task 3's automated `deploy-smoke.sh` check confirming the live app actually serves the new build (per `01-RESEARCH.md` Pitfall 7). |
| T-01-SC (repeat) | Tampering | No new npm installs occur in this plan | n/a | n/a | Already mitigated in Plan 01 (Task 1's package-legitimacy checkpoint); no new packages are introduced here. |
</threat_model>

<verification>
1. Task 1's decision checkpoint is resolved with an explicit option + domain/subpath string + repo name.
2. `npm run build` succeeds with the finalized `base`.
3. The new GitHub repo exists and contains this project's pushed source.
4. Task 3's manual cPanel checkpoint is confirmed "deployed" with a live URL.
5. `scripts/deploy-smoke.sh` passes against the live URL.
</verification>

<success_criteria>
- The site is live at its MilesWeb production URL.
- The live site passes the same smoke test that verified the local build in Plan 01.
- ROADMAP.md Phase 1's Success Criterion 5 ("The site is live at its MilesWeb production URL, deployed via the proven jagdamba_automobiles dependency-free Node app.js + Vite dist/ pattern") is satisfied — completing the phase.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-content-deployment/01-04-SUMMARY.md` when done
</output>
