---
phase: 01-app-shell-content-deployment
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/content/projects.ts
  - src/content/testimonials.ts
  - src/sections/Projects.tsx
  - src/sections/Testimonials.tsx
  - src/sections/Projects.test.tsx
  - src/sections/Testimonials.test.tsx
  - src/App.tsx
  - src/nav/TopNav.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, SOCIAL-01]
must_haves:
  truths:
    - "Visitor can browse exactly 6 curated project cards (description, tech tags, GitHub link) sourced from the D-02 locked repo list (PROJ-01)."
    - "Project card data is static, hand-curated at build time; no runtime GitHub API/fetch call is ever made (PROJ-02)."
    - "No concurrent-request or interruption scenario exists for Projects since there is no runtime data fetch to interrupt (PROJ-02 concurrency edge, resolved)."
    - "Each project card links to its own GitHub repo with target=_blank rel=noopener noreferrer (PROJ-03)."
    - "Project cards never merge/overlap regardless of viewport width; a grid/wrap layout keeps them visually distinct (PROJ-03 adjacency edge, resolved)."
    - "Every project card has a non-empty description and a non-empty tech-tag list (PROJ-03 empty edge, resolved)."
    - "Project cards render in fixed D-02 source order: acksync, acksync_crm_lmb, stock_predictor, jagdamba_automobiles, cbse_tutor, compliOS (PROJ-03 ordering edge, resolved)."
    - "Visitor can read exactly 3 verbatim recommendation quotes with name+company attribution (SOCIAL-01)."
    - "The full 9-section app shell (Hero, About, Experience, Projects, Skills, Certifications, Education, Testimonials, Contact) renders in the RESEARCH.md-recommended order, with TopNav listing all 9 anchors."
  artifacts:
    - src/content/projects.ts
    - src/content/testimonials.ts
    - src/sections/Projects.tsx
    - src/sections/Testimonials.tsx
    - src/sections/Projects.test.tsx
    - src/sections/Testimonials.test.tsx
  key_links:
    - "App.tsx's final section order: Hero → About → Experience → Projects → Skills → Certifications → Education → Testimonials → Contact"
    - "TopNav's links array has all 9 anchor entries after this plan"
  prohibitions:
    - statement: "Projects section must never call any external API (GitHub or otherwise) at runtime (PROJ-02)."
      status: "descriptor-less"
      verification: "grep -rn 'fetch(\\|api.github.com\\|axios' src/sections/Projects.tsx src/content/projects.ts returns no matches"
  backstop:
    - statement: "Testimonials/Projects counts (3/6) are locked for v1, but the grid/list layout uses auto-fit/wrap CSS, not a hardcoded column count — untested against any count other than the locked ones."
      verification: "backstop"
    - statement: "Testimonial quotes render at natural, unclamped height (Rebecca Cengiz-Robbs's quote is noticeably longer than Nathan Stewart's); the grid must use align-items:start so cards don't stretch to match their tallest sibling — pending final visual confirmation against the actual rendered copy length."
      verification: "backstop"
---

<objective>
Add the two remaining "curated judgment" content sections — Projects (6 cards drafted from each repo's actual content, not name-only guesses) and Testimonials (3 verbatim recommendation quotes) — completing the full 9-section content shell and finalizing App.tsx/TopNav.tsx assembly.

Purpose: Delivers PROJ-01/02/03 and SOCIAL-01, and closes out the content-authoring half of the phase (Plan 04 is deployment-only from here).

Output: `src/content/{projects,testimonials}.ts` + matching `src/sections/*.tsx` + tests; final `App.tsx` with all 9 sections in RESEARCH.md's recommended order; final `TopNav.tsx` with all 9 anchor links.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-app-shell-content-deployment/01-CONTEXT.md
@.planning/phases/01-app-shell-content-deployment/01-UI-SPEC.md
@.planning/phases/01-app-shell-content-deployment/01-RESEARCH.md
@.planning/phases/01-app-shell-content-deployment/01-02-SUMMARY.md
</context>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Notes |
|---|---|---|
| `src/content/projects.ts` | content | `Project` interface (`name, description, tags: string[], repoUrl`) + `projects: Project[]` (6 entries, D-02 fixed order) |
| `src/content/testimonials.ts` | content | `Testimonial` interface (`quote, name, title`) + `testimonials: Testimonial[]` (3 entries) |
| `src/sections/Projects.tsx` | component | renders `projects` as a card grid in `<section id="projects">` |
| `src/sections/Testimonials.tsx` | component | renders `testimonials` as quote cards in `<section id="testimonials">` |
| `src/sections/Projects.test.tsx`, `src/sections/Testimonials.test.tsx` | tests | render + content/link assertions |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Projects</name>
  <files>src/content/projects.ts, src/sections/Projects.tsx, src/sections/Projects.test.tsx, src/App.tsx, src/nav/TopNav.tsx</files>
  <read_first>
    - `.planning/phases/01-app-shell-content-deployment/01-UI-SPEC.md` — project-card grid/wrap layout tokens, hover-glow treatment (CSS-only per Plan 01's Dependency Decisions — no Framer Motion).
    - `src/sections/Contact.tsx` (Plan 01) — the `target="_blank" rel="noopener noreferrer"` external-link pattern to replicate for each project card's GitHub link.
  </read_first>
  <behavior>
    - `Projects.test.tsx`: renders `<Projects />`, asserts exactly 6 project names from `projects` appear in DOM order matching the array's authored order, asserts every card's GitHub link has `target="_blank"` and `rel="noopener noreferrer"`, and asserts every project has a non-empty `description` and at least one tag in `tags`.
  </behavior>
  <action>
    Create `src/content/projects.ts`: `export interface Project { name: string; description: string; tags: string[]; repoUrl: string }` and `export const projects: Project[] = [...]` with exactly these 6 entries, in this fixed order (D-02 locked list, all under `github.com/Sandyzie05`):

    1. `{ name: 'acksync', description: 'Self-hosted single-page marketing site for ACKSYNC INFOTECH featuring a light/dark theme system, Framer Motion animations, and a backend-free mailto contact flow.', tags: ['React', 'TypeScript', 'Vite', 'Tailwind CSS'], repoUrl: 'https://github.com/Sandyzie05/acksync' }`
    2. `{ name: 'acksync_crm_lmb', description: 'Cross-platform offline-first POS and CRM desktop app for a retail sweet shop, with GST-compliant billing, a touch-friendly UI, and local SQLite storage.', tags: ['Tauri 2', 'React', 'TypeScript', 'Rust', 'SQLite'], repoUrl: 'https://github.com/Sandyzie05/acksync_crm_lmb' }`
    3. `{ name: 'stock_predictor', description: 'FastAPI backend for evidence-linked stock research, mapping market moves to news and filings with an AI-infrastructure theme tracker and self-auditing prediction-accuracy reports.', tags: ['Python', 'FastAPI', 'PostgreSQL', 'Docker'], repoUrl: 'https://github.com/Sandyzie05/stock_predictor' }`
    4. `{ name: 'jagdamba_automobiles', description: 'React storefront and admin platform for a motorcycle-parts retailer, with live inventory search, an image-driven parts gallery, and GitHub-API-backed admin publishing.', tags: ['React', 'TypeScript', 'Vite', 'Node.js'], repoUrl: 'https://github.com/Sandyzie05/jagdamba_automobiles' }`
    5. `{ name: 'cbse_tutor', description: 'Multi-agent RAG chatbot teaching the CBSE Grade 5 curriculum, pairing each subject with its own dedicated AI agent and an isolated knowledge base built from official textbooks.', tags: ['Python', 'FastAPI', 'Ollama', 'ChromaDB'], repoUrl: 'https://github.com/Sandyzie05/cbse_tutor' }`
    6. `{ name: 'compliOS', description: 'Full-stack compliance workflow platform for Chartered Accountant firms, tracking companies, directors, and multi-step regulatory filings through a REST API and React UI.', tags: ['FastAPI', 'PostgreSQL', 'React', 'Docker'], repoUrl: 'https://github.com/Sandyzie05/compliOS' }`

    Create `src/sections/Projects.tsx`: `<section id="projects" aria-labelledby="projects-heading">` mapping `projects` to a CSS grid of cards (`grid-template-columns: repeat(auto-fit, minmax(...))` per `01-UI-SPEC.md`'s token — never a fixed 6-column layout, so the grid never overlaps/merges regardless of viewport). Each card renders `name`, `description`, `tags` as a wrapped row of chip `<span>`s, and a `<a href={repoUrl} target="_blank" rel="noopener noreferrer">View on GitHub</a>` link. No `fetch`/`axios`/API call anywhere in this component — all data comes from the imported `projects` const (PROJ-02).

    Update `src/App.tsx`: insert `<Projects />` immediately after `<Experience />` and before `<Skills />` (final full order: Hero → About → Experience → Projects → Skills → Certifications → Education → Testimonials → Contact, per `01-RESEARCH.md`'s recommended section order).

    Update `src/nav/TopNav.tsx`'s `links` array: insert `{ href: '#projects', label: 'Projects' }` between the Experience and Skills entries.
  </action>
  <verify>
    <automated>npm test -- --run src/sections/Projects.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "repoUrl:" src/content/projects.ts` returns `6`.
    - `grep -c "api.github.com\|fetch(" src/sections/Projects.tsx src/content/projects.ts` returns `0`.
    - `grep -c 'target="_blank"' src/sections/Projects.tsx` returns `1` (single template-rendered anchor pattern, applied to all 6 cards via the map).
    - `npm test -- --run src/sections/Projects.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Projects renders all 6 cards in D-02's fixed order with safe external links, zero runtime API calls, wired into App.tsx/TopNav.tsx, test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testimonials + full-shell assembly verification</name>
  <files>src/content/testimonials.ts, src/sections/Testimonials.tsx, src/sections/Testimonials.test.tsx, src/App.tsx, src/nav/TopNav.tsx</files>
  <read_first>
    - `.planning/phases/01-app-shell-content-deployment/01-CONTEXT.md` `<specifics>` section — the 3 locked, verbatim recommendation quotes with exact attribution.
    - `.planning/phases/01-app-shell-content-deployment/01-UI-SPEC.md` — testimonial quote-card styling token and its backstop note on unclamped quote height.
  </read_first>
  <behavior>
    - `Testimonials.test.tsx`: renders `<Testimonials />`, asserts all 3 quotes and all 3 name+title attributions from `testimonials` appear in the rendered output, in the array's authored order.
  </behavior>
  <action>
    Create `src/content/testimonials.ts`: `export interface Testimonial { quote: string; name: string; title: string }` and `export const testimonials: Testimonial[] = [...]` with exactly these 3 entries, verbatim per D-03/D-04 (praise text kept exactly as supplied, only LinkedIn UI chrome trimmed):

    1. `{ quote: "Sandeep is an amazing colleague. He is filled with outstanding leadership qualities. His attitude towards work and his co-workers is commendable. He is strong in will power to take the very first step to change the monotonous system.", name: 'Ankit Agnihotri', title: 'Senior Site Reliability Engineer, Adobe' }`
    2. `{ quote: "Sandeep has great passion to learn and fulfill his responsibilities to the best of his ability. While working on my team, he never ceased to give 100 percent effort until the task or project was complete.", name: 'Nathan Stewart', title: 'Lead DevOps Engineer, Five9' }`
    3. `{ quote: "Sandeep is one of the most motivated and hardworking engineers I know. I never had to worry about his daily work or projects. He owned his work completely and consistently met deadlines and provided excellent communication and support to our internal customers. He invested many hours of his personal time to research and learn new technologies to help him succeed. It was an honor to work with Sandeep.", name: 'Rebecca Cengiz-Robbs', title: 'IT Infrastructure & Project Management Professional' }`

    Create `src/sections/Testimonials.tsx`: `<section id="testimonials" aria-labelledby="testimonials-heading">` mapping `testimonials` to a row/grid of quote cards, each rendering the `quote` text, then `name` and `title`. Use `align-items: start` on the grid/flex container (per `01-UI-SPEC.md`'s backstop note) so the visibly longer Rebecca Cengiz-Robbs quote does not force the shorter cards to stretch.

    Update `src/App.tsx` to its final Phase-1 section order: `<TopNav />`, then `<main id="main">` containing, in order, `<Hero />`, `<About />`, `<Experience />`, `<Projects />`, `<Skills />`, `<Certifications />`, `<Education />`, `<Testimonials />`, `<Contact />`, then `<footer>`.

    Update `src/nav/TopNav.tsx`'s `links` array to its final 9-entry state, in section order: Home (`#hero`), About, Experience, Projects, Skills, Certifications, Education, Testimonials, Contact — matching `App.tsx`'s final section order exactly.
  </action>
  <verify>
    <automated>npm test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "quote:" src/content/testimonials.ts` returns `3`.
    - `grep -c "<section id=" src/App.tsx` returns `9`.
    - `grep -c "href: '#" src/nav/TopNav.tsx` returns `9`.
    - `npm test -- --run` (full suite, all plans) exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Testimonials renders all 3 verbatim quotes; App.tsx contains all 9 sections in the correct final order; TopNav.tsx lists all 9 matching anchors; full test suite and build both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Visitor → project-card GitHub links | New `target="_blank"` external-link surface (6 new instances) — same reverse-tabnabbing class as Plan 01's Contact links |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|------------|----------|-------------|-----------------|
| T-01-05 | Spoofing | Reverse tabnabbing via `target="_blank"` on Project card GitHub links | medium | mitigate | `rel="noopener noreferrer"` on every project card link (Task 1, verified by acceptance-criteria grep — same pattern as T-01-03 in Plan 01). |
| T-01-06 | Information Disclosure | Testimonial quotes include real names/companies of third-party recommenders | low | accept | D-03/D-04 confirm these quotes were supplied by the user with intent to publish on the portfolio; they originate from the recommenders' own public LinkedIn recommendations, already visible to their professional network — the user is the appropriate data controller for republishing this professional-context content. |
</threat_model>

<verification>
1. `npm test -- --run` (full suite across all 3 plans so far) passes.
2. `npm run build` exits 0.
3. Manual read of `src/App.tsx` confirms all 9 `<section id="...">` elements in the RESEARCH.md-recommended order.
4. Manual read of `src/nav/TopNav.tsx` confirms 9 anchor entries matching the 9 section ids exactly.
</verification>

<success_criteria>
- A visitor can browse all 6 project cards with working, safe GitHub links, and read all 3 testimonial quotes with attribution.
- The full 9-section app shell (Hero, About, Experience, Projects, Skills, Certifications, Education, Testimonials, Contact) is assembled and navigable end-to-end via TopNav.
- This closes ROADMAP.md Phase 1 Success Criteria 1-4; only Success Criteria 5 (live deployment) remains, addressed by Plan 04.
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-content-deployment/01-03-SUMMARY.md` when done
</output>
